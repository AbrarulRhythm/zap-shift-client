import React from 'react';
import Logo from '../../../components/Logo/Logo';

const NavBar = () => {
    return (
        <div>
            <Logo></Logo>
        </div>
    );
};

export default NavBar;